[project]
name = "navigen-ugv-simulation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.116,<1.0", "uvicorn[standard]>=0.35,<1.0", "pillow>=11,<13"]

[tool.uv]
package = false
