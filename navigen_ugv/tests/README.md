# Tests

Unit tests live inside each ROS package (`colcon test`). This directory is reserved for
cross-package integration test scenarios (Phase 3+): bag-replay regression tests and
simulation acceptance runs (Point A → Point B without collision).
