# Simulation models

Custom Gazebo Harmonic models (rocks, trees, ramps). The world file
`simulation/worlds/navigen_outdoor.sdf` currently uses inline primitive obstacles so no
external downloads are required. Add SDF models here and reference them via
`GZ_SIM_RESOURCE_PATH` (set automatically by sim.launch.py).
